{
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^3.3.2",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "eslint": "^8.41.0",
    "prettier": "2.8.8"
  },
  "scripts": {
    "start": "nodemon src/server.js",
    "format": "npx prettier --write . --ignore-path .gitignore",
    "lint": "eslint . --ignore-path .gitignore",
    "fix-style": "npm run format && npm rin lint"
  }
}
